<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Task</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      padding: 20px;
    }
    form {
      background: #fff;
      padding: 20px;
      border-radius: 8px;
      max-width: 500px;
      margin: auto;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    input, textarea, select, button {
      display: block;
      width: 100%;
      margin-bottom: 15px;
      padding: 10px;
      border-radius: 4px;
      border: 1px solid #ccc;
    }
    button {
      background-color: #007bff;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background-color: #007bff;
    }
  </style>
</head>
<body>

  <h1 style="text-align:center;">Create New Task</h1>
  <form id="taskForm" method="POST">
    <input type="text" name="title" placeholder="Title" required minlength="3" maxlength="100" />
    <textarea name="description" placeholder="Description" maxlength="500"></textarea>
    <input type="date" name="dueDate" min="<?= date('Y-m-d'); ?>" />
  
    <select name="status" required>
      <option value="">Select Status</option>
      <option value="Pending">Pending</option>
      <option value="In Progress">In Progress</option>
      <option value="Completed">Completed</option>
    </select>
  
    <select name="priority" required>
      <option value="">Select Priority</option>
      <option value="Low">Low</option>
      <option value="Medium">Medium</option>
      <option value="High">High</option>
    </select>
  
    <input type="text" name="category" placeholder="Category" maxlength="50" />
    <input type="text" name="tags" placeholder="Tags (comma-separated)" pattern="^([a-zA-Z0-9\s]+)(,[a-zA-Z0-9\s]+)*$" title="Comma-separated words only" />
    <input type="hidden" name="userId" value="userId" />
  
    <!-- Buttons with separate actions -->
    <button type="submit" formaction="/tasks/create">Create Task</button>
    <button type="submit" formaction="/tasks/update">Update Task</button>
    <button type="submit" formaction="/tasks/delete">Delete Task</button>
  </form>
  
</body>
</html>
